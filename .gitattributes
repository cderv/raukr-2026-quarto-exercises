# The exercises repo ships the reference solutions LOCALLY (in-room resilience: nobody is stranded
# by the break, and a stuck participant with flaky Wi-Fi has a local reference, not a re-download).
# Withholding is spatial -- solutions/ is a separate top-level folder with a "try first" framing.
#
# THE HARD-STRIP DIAL: uncomment the line below to strip solutions/ from the use_course()/codeload
# ZIP for a stricter audience (a future edition that should not ship answers). `export-ignore` drops
# the path from `git archive` -- which is exactly what codeload.github.com serves to use_course() and
# to the browser "Download ZIP" button. Local clones still get solutions/; the download does not.
#
# solutions/ export-ignore
