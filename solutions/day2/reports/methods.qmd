---
title: "Methods"
description: "How the penguins were measured, and what we drop before analysing."
---

```{r}
#| label: setup
#| message: false
library(dplyr)
data(penguins)
```

Measurements come from three species at Palmer Station, Antarctica. Each row is one bird: bill
length, bill depth, flipper length, and body mass.

A few birds are missing one or more measurements. We drop those rows before analysing, so every
figure and table on this site uses the same set of complete cases.

```{r}
#| label: tbl-complete
#| tbl-cap: "Rows kept per species after dropping incomplete measurements."
penguins |>
  filter(!is.na(bill_len), !is.na(bill_dep), !is.na(body_mass)) |>
  count(species, name = "complete_rows") |>
  knitr::kable()
```
